# Local-only archive of unrelated files from prior experiments
# (Trigger.dev workflows, WAT framework, etc.) — kept for reference
# but not part of the public portfolio repo.
_archive/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node / build artifacts (in case we add a build step later)
node_modules/
dist/
build/

# Local screenshots from design iteration
temporary screenshots/

# Secrets — never commit
.env
.env.*
*.key

# Local-only progress / preview files
progress-*.md
projects/preview-*.html

# Image generation script (uses API key) and any local-only generation logs
gen-infographics.py
gen-infographics.log
